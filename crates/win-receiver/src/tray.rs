//! (Kaldırıldı) Eski `tao` + `tray-icon` + `muda` tepsisi.
//! Yerini Slint tabanlı `gui.rs` (native SystemTrayIcon + ayar penceresi) aldı.
//! Bu dosya artık derlenmiyor (main.rs'te `mod tray;` yok) ve silinebilir.
