# Rust
/target
**/target

# Distribution outputs (produced by the packaging scripts; not committed)
/dist

# Installers on the landing page (copied from CI/dist; kept out of git)
site/downloads/*.dmg
site/downloads/*.msi

# Icon intermediate output (make_icon.py produces it; the .icns/.png sources are committed)
*.iconset/

# dmg background intermediates (make_dmg_background.py; the retina .tiff is committed)
packaging/mac/dmg-background.png
packaging/mac/dmg-background@2x.png

# macOS
.DS_Store

# Python (packaging/mac/make_icon.py)
__pycache__/

# kalfa plugin state files (not part of this project)
.kalfa/
**/.kalfa/

# Cargo.lock: committed on purpose (these are binary crates), so not ignored here.

# wrangler local cache (deploy tool; kept out of the repo)
.wrangler/
