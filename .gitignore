# Rust
/target
**/target

# Dağıtım çıktıları (paketleme scriptleri üretir; commit edilmez)
/dist

# İkon ara-çıktısı (make_icon.py üretir; .icns/.png kaynağı commit edilir)
*.iconset/

# macOS
.DS_Store

# kalfa eklentisi durum dosyaları (bu projeye ait değil)
.kalfa/
**/.kalfa/

# Cargo.lock: binary/uygulama olduğu için commit EDİLİR (bilerek ignore edilmedi).
